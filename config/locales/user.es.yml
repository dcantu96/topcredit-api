es:
  activerecord:
    models:
      user: "Usuario"
    attributes:
      user:
        state: "Estado"
        email: "Correo Electrónico"
        status: "Estatus"
        invitation_token: "Código de Invitación"
    errors:
      models:
        user:
          attributes:
            invitation_token:
              invalid: "no es válido"
            status:
              inclusion: "%{value} no es válido"
            state:
              inclusion: "%{value} no es válido"
            email:
              taken: "ya está en uso"
              invalid_domain: "%{value} es un dominio no permitido"
